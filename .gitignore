__pycache__/
*.pyc
# dist/ holds generated files, BUT catalog.json and artist_index.json are the
# product itself — the app pulls them from a CDN, so they must be in the repo.
dist/*
!dist/catalog.json
!dist/artist_index.json
!dist/stats.json
.venv/
.DS_Store
